{
  "type": "object",
  "definitions": {
    "EcdhWebcryptoTestGroup": {
      "type": "object",
      "properties": {
        "type": {
          "enum": [
            "EcdhWebcryptoTest"
          ]
        },
        "source": {
          "$ref": "common.json#/definitions/Source"
        },
        "curve": {
          "type": "string",
          "format": "EcCurve",
          "description": "the name of the curve"
        },
        "encoding": {
          "type": "string",
          "description": "the encoding of the public key"
        },
        "tests": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/EcdhWebcryptoTestVector"
          }
        }
      },
      "required": ["type", "source", "curve", "encoding", "tests"],
      "additionalProperties": false
    },
    "EcdhWebcryptoTestVector": {
      "type": "object",
      "properties": {
        "tcId": {
          "type": "integer",
          "description": "Identifier of the test case"
        },
        "comment": {
          "type": "string",
          "description": "A brief description of the test case"
        },
        "public": {
          "type": "object",
          "description": "Public key in webcrypto format"
        },
        "private": {
          "type": "object",
          "description": "Private key in webcrypto format"
        },
        "shared": {
          "type": "string",
          "format": "HexBytes",
          "description": "The shared secret key"
        },
        "result": {
          "$ref": "common.json#/definitions/Result"
        },
        "flags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "A list of flags"
        }
      },
      "required": ["tcId", "comment", "public", "private", "shared", "result", "flags"],
      "additionalProperties": false
    }
  },
  "properties": {
    "algorithm": {
      "type": "string",
      "description": "the primitive tested in the test file"
    },
    "generatorVersion": {
      "type": "string",
      "description": "DEPRECATED: prefer \"source\" property in test group",
      "deprecated": true
    },
    "header": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "additional documentation"
    },
    "notes": {
      "$ref": "common.json#/definitions/Notes"
    },
    "numberOfTests": {
      "type": "integer",
      "description": "the number of test vectors in this test"
    },
    "schema": {
      "enum": [
        "ecdh_webcrypto_test_schema_v1.json"
      ]
    },
    "testGroups": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/EcdhWebcryptoTestGroup"
      }
    }
  },
  "required": ["algorithm", "header", "notes", "numberOfTests", "schema", "testGroups"],
  "additionalProperties": false
}
